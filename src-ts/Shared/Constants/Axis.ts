enum Axis {
    X = 'X',
    Y = 'Y',
    BOTH = 'BOTH',
    NONE = 'NONE'
}

const AxisStore = {
    X: Axis.X,
    Y: Axis.Y,
    BOTH: Axis.BOTH,
    NONE: Axis.NONE
};

export {
    AxisStore,
    Axis as default
};